/**
 * @deprecated Fictional demo stories were removed from the site.
 * To purge any leftovers from Sanity, run: npm run delete:seed-stories
 */
console.error(
  "seed:stories is disabled — demo stories were removed. Run: npm run delete:seed-stories",
);
process.exit(1);
